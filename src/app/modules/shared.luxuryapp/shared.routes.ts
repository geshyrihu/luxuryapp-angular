import { Routes } from "@angular/router";

// Sin rutas propias: `shared.luxuryapp` expone catalogos/piezas reutilizables
// consumidas por otros modulos; no declara navegacion propia.
export const sharedRoutes: Routes = [];
