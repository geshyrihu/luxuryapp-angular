import { DOCUMENT_TYPE_MAP } from './document-types.map';

describe('Document Types Map', () => {
  it('DOCUMENT_TYPE_MAP should exist', () => {
    expect(DOCUMENT_TYPE_MAP).toBeDefined();
  });
});
