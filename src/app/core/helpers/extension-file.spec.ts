import { getIconForFileHelper } from './extension-file';

describe('extension-file helpers', () => {
  it('getIconForFileHelper should exist', () => {
    expect(getIconForFileHelper).toBeDefined();
  });
});
