import { globalFilterFields, tablePrimeNgRows, rowsPerPageOptions, rowsTablePrimeNg } from './table-primeng-option';

describe('tablePrimengOption helpers', () => {
  it('globalFilterFields should exist', () => {
    expect(globalFilterFields).toBeDefined();
  });

  it('tablePrimeNgRows should exist', () => {
    expect(tablePrimeNgRows).toBeDefined();
  });

  it('rowsPerPageOptions should exist', () => {
    expect(rowsPerPageOptions).toBeDefined();
  });

  it('rowsTablePrimeNg should exist', () => {
    expect(rowsTablePrimeNg).toBeDefined();
  });
});
