import { TICKET_STATUS_SEVERITY } from './status';

describe('Ticket Status constants', () => {
  it('TICKET_STATUS_SEVERITY should exist', () => {
    expect(TICKET_STATUS_SEVERITY).toBeDefined();
  });
});
