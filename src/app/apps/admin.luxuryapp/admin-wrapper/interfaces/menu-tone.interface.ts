export interface MenuTone {
  card: string;
  accent: string; // hex Ǹ border-top y color de icono (alineado con master-dashboard)
  bgColor: string; // hex Ǹ fondo del contenedor de icono
  desktopShell: string;
  mobileShell: string;
  glyph: string;
  heading: string;
}
